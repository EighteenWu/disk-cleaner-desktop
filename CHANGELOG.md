# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。版本号遵循语义化版本。

安装包不包含 API Key。密钥只保存在本机 Windows 凭据管理器，不要把 Key、`.env`、扫描库存 SQLite 或 `.pi/` 会话文件提交到 Git。

## [0.2.0] - 2026-08-26

### 新增

- AI 规则对话：发送后流式回复；点「通过」才生成规则、写入本地规则库并启用。
- 全盘分析完成后，对话发送占用最大的一批目录摘要（不是每个文件），AI 按 **大小 / 路径 / 说明 / 影响** 分成建议可清、要确认、不要动。
- 推荐规则包与扫描侧栏低 / 中 / 高清理档位。
- NTFS 全盘分析优先解析 `$MFT`；空间分析工作台可浏览占用最大的目录。
- 规则表：停用后可再启用；删除即从列表消失，不能「恢复为禁用」。

### 变更

- 去掉「准备发送预览」和 AI 草稿 JSON 编辑器主路径。
- AI 批准不再只生成待批准草稿，通过即启用；全库只维护一条 AI 生成规则，再次通过会覆盖。
- 快速扫描只走已批准的本地规则路径；推荐 YAML 不会在启动时静默加载。

### 安全

- 发给模型的是脱敏后的候选项摘要，加上（若有）占空间目录模板路径与大小，不发送文件内容。
- API Key 仍只进 Windows 凭据管理器；日志和错误不包含密钥。
- `blocked`、强制排除和 `DeleteStrategy::Skip` 不能被对话放宽。通过不等于执行清理。

## [0.1.0] - 2026-08

- 首个桌面版本：扫描、规则库、清理预览、BYOK 供应商配置。
